import React, { FC } from 'react'

interface typeProps {
  currentPage: number,
  totalPage: number
}

const Pages: FC<typeProps> = (props: typeProps) => {
  return (
    <div></div>
  )
}

export default Pages